const singleQuote = 'string';
const doubleQuote = 'string';
const backticks = `string`;
const str2 = new String(`string`);

//Methods
const tweet = `Hello this is part of tweeter`;

// length
console.log(tweet.length);

// index of -> position
console.log(tweet.indexOf('Hello'));

// includes -> boolean
console.log(tweet.includes('this'));
